.theme-container {
  font-family: 'Fira Sans'

  .hero{
    img {
      width: 100%;
      object-fit: cover;
      margin: 0 0 0 0;
    }
  }
  .home {
    max-width: 100%;

    .features {
      max-width: 960px;
      margin: 0px auto;
      padding: 0.5rem 1rem;
    }
    .theme-default-content {
      max-width: 960px;
      margin: 0px auto;
      padding: 0.5rem 1rem;
    }

  }
}
 
